# MTG Sets

Sets released by Wizards of the Coast.

Mainly to host pictures removed on Scryfall.

## How to install?

* Drop the `custom` folder in `%appdata%\Forge`
* Drop the `pics` folder in `%localappdata%\Forge\cache`
